// Keeping it just because the syntax highlighting doesn't work without and setting a custom one is experimental in the extension

const config = require("./themeConfig");

module.exports = { ...config };
